version: "3"
services:
  db:
    image: 'postgres:alpine'
    container_name: db
    ports:
      - "5432:5432"
    environment:
        - POSTGRES_USER=java_test_app
        - POSTGRES_PASSWORD=password
